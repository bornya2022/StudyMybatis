<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java_study.mybatis.mapper.system.WorkCardMapper">
    <select id="getWorkCardByEmpId" parameterType="long" resultType="com.java_study.mybatis.model.system.WorkCard">
        select id,emp_id,real_name,department,moblie,POSITION,note from t_work_card where emp_id=#{empId}
    </select>
</mapper>