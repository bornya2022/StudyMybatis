<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java_study.mybatis.mapper.system.MaleHealthFormMapper">
    <select id="getMaleHealthForm" parameterType="long" resultType="com.java_study.mybatis.model.system.MaleHealthForm">
        select id,heart,liver,spleen,lung,kideny,prostate,note from t_male_health_from where emp_id=#{id}
    </select>
</mapper>